# Local development defaults; change before deploying outside localhost.
# Use a URL-safe password, or URL-encode it in a custom DATABASE_URL.
POSTGRES_PASSWORD=odin
DB_PORT=5432
API_PORT=8000
FRONTEND_PORT=3000
# auto: use Anthropic when a key exists, otherwise explicitly reported mock mode.
AI_MODE=auto
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
